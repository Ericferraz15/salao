django>=6.0,<7.0
psycopg2-binary>=2.9
gunicorn>=21.2
whitenoise>=6.6
python-dotenv>=1.0
pillow>=10.0
# Login com Google: valida a credencial (JWT) que o botão do Google devolve.
# requests é o transporte que a google-auth usa para baixar as chaves públicas.
google-auth>=2.0
requests>=2.31
